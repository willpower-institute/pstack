services:
  app:
    build: .
    container_name: pstack-app
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - default
      # deploy จริงบนเซิร์ฟเวอร์ (หลัง Caddy) ให้เปิดบรรทัดล่างและลบ ports: ด้านบน
      # ⚠️ อย่าลืมตั้ง FORWARDED_ALLOW_IPS ใน .env ให้เป็น IP ของ Caddy ด้วย
      #    ไม่งั้น rate limit ต่อ IP จะกลายเป็นลิมิตรวมทั้งระบบ (ดู .env.example)
      # - odoo-public

  worker:
    build: .
    container_name: pstack-worker
    restart: unless-stopped
    # `python -m arq` ใส่ CWD (/app) ลง sys.path — resolve core/addons จาก /app
    # (arq เป็น console script ไม่ใส่ CWD ให้ ต่างจาก uvicorn → worker บูตไม่ขึ้น #4)
    command: python -m arq core.worker.WorkerSettings
    env_file: .env
    volumes:
      - uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - default

  # ⚠️ ${DB_USER} คือ bootstrap superuser ของ cluster (rolsuper + rolbypassrls)
  # ถ้าใช้โมดูล tenancy + RLS ห้ามให้ app ต่อ DB ด้วย role นี้ — RLS จะไม่มีผลเลย
  # สร้าง role แยกด้วย deploy/db-role.sql แล้วชี้ PSTACK_DATABASE_URL มาที่ role นั้น
  db:
    image: postgres:16-alpine
    container_name: pstack-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - default
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: pstack-redis
    restart: unless-stopped
    networks:
      - default

volumes:
  pgdata:
  uploads:

networks:
  default:
    name: pstack_default
  # odoo-public:
  #   external: true
